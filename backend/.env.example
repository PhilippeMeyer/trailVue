# Copy to backend/.env and fill in. .env is git-ignored — never commit it.

# Komoot account used to sync recorded tours
KOMOOT_EMAIL=you@example.com
KOMOOT_PASSWORD=your-komoot-password

# Port the express API listens on (default 5000)
PORT=5000
